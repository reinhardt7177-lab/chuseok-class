# 비밀 — 절대 올리지 않는다
.env
.env.local

node_modules/
*.log
scripts/.asset-cache.json

# 삽화와 낱개 그림은 올린다.
# 생성물이지만 이게 없으면 배포된 앱이 빈 화면이 되고,
# 다시 만들려면 Leonardo 크레딧이 또 든다.

# 코덱스가 그림을 다듬으며 만드는 작업 파일 — 저장소에 넣지 않는다
.codex-*
